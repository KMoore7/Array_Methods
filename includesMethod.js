//This method does not use a function. It simply checks to see if an array has an element without doing an extensive check.
//Returns true or false

const items = [1,2,3,4,5]

const includesTwo = items.includes(2)

console.log(includesTwo)
