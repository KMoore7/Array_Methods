// Create a method that accepts a list and an item, and returns true if the item belongs to the list, otherwise false.



function include(arr, item){ //create a function named inlcude with 2 parameters: arr, item
    return arr.includes(item); //use the include function to reutrn true or false if parameter arr includes the item item
   }
